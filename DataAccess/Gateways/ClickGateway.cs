/*  Generated by CodeGen written by Concord Mfg.
 * Transform file used: BEGateway (v0.2.0.0).xslt
 * Date generated: 3/28/2012 12:46:00 PM
 * CodeGen version: 0.2.0.0  */

using System;
using System.Configuration;
using System.Collections.Generic;
using System.Linq;
using System.Text.RegularExpressions;

namespace ConcordMfg.PremierSeniorSolutions.WebService.DataAccess
{
	public class ClickGateway : GatewayBase, IClickGateway
	{
		#region IClickGateway Members
		public IOrderedQueryable<Click> GetAll()
		{
			try
			{
				using (PSS2012DataContext context = this.DataContext)
				{
					return context.Clicks.OrderBy(click => click.ClickGuid);
				}
			}
			catch (System.Data.SqlClient.SqlException ex)
			{
				throw this.HandleSqlException(ex);
			}
		}

		public List<Click> GetAllWithUndefined()
		{
			Click undefinedClick = new Click()
			{
				ClickGuid = Guid.Empty,
				FacilityGuid = Guid.Empty,
				ListingTypeGuid = Guid.Empty,
				TimeStamp = default(DateTime),
			};

			List<Click> response = this.GetAll().ToList();
			response.Add(undefinedClick);

			return response;
		}

		public Click GetByPK(Guid clickGuid)
		{
			if (Guid.Empty == clickGuid)
			{ return new Click(); }

			try
			{
				Click daClick = new Click();
				using (PSS2012DataContext context = this.DataContext)
				{
					daClick = (
						from items in context.Clicks
						where items.ClickGuid == clickGuid
						select items).SingleOrDefault();
				}

				if (null == daClick)
				{
					throw new DataAccessException("Click no longer exists.");
				}

				return daClick;
			}
			catch (System.Data.SqlClient.SqlException ex)
			{
				throw this.HandleSqlException(ex);
			}
		}

		/// <summary>
		/// Inserts click business entity into the data store.
		/// </summary>
		/// <param name="entity">The click business entity to insert.</param>
		/// <returns>The click identifier.</returns>
		public Click Insert(Click entity)
		{
			//@@NEW - changed return type to entity type.
			try
			{
				using (PSS2012DataContext context = DataContext)
				{
					entity.ClickGuid = Guid.NewGuid();

					context.Clicks.InsertOnSubmit(entity);
					context.SubmitChanges();
				}

				//@@NEW - returning full entity.
				return entity;
			}
			catch (System.Data.SqlClient.SqlException ex)
			{
				throw this.HandleSqlException(ex);
			}
		}

		public void Update(Click entity)
		{
			if (Guid.Empty == entity.ClickGuid)
				throw new PrimaryKeyMissingException("Click", entity, "update");

			try
			{
				using (PSS2012DataContext context = DataContext)
				{
					// Get the entity to update.
					Click clickToUpdate = GetByPK(entity.ClickGuid);

					// Set the new values.
					clickToUpdate.FacilityGuid = entity.FacilityGuid;
					clickToUpdate.ListingTypeGuid = entity.ListingTypeGuid;
					clickToUpdate.TimeStamp = entity.TimeStamp;

					// Perform the update.
					context.SubmitChanges();
				}
			}
			catch (System.Data.SqlClient.SqlException ex)
			{
				throw this.HandleSqlException(ex);
			}
		}

		public void Delete(Guid clickGuid)
		{
			if (Guid.Empty == clickGuid)
				throw new PrimaryKeyMissingException("Click", clickGuid, "delete");


			try
			{
				using (PSS2012DataContext context = DataContext)
				{
					// Get the entity to delete.
					Click clickToDelete = GetByPK(clickGuid);

					// Peform the delete.
					context.Clicks.DeleteOnSubmit(clickToDelete);
					context.SubmitChanges();
				}
			}
			catch (System.Data.SqlClient.SqlException ex)
			{
				throw this.HandleSqlException(ex);
			}
		}

		public IOrderedQueryable<Click> GetForFacilityByFacilityGuid(Guid facilityGuid)
		{
			try
			{
				using (PSS2012DataContext context = DataContext)
				{
					// Get the entity to delete.
					return
						(from items in context.Clicks
						 where items.FacilityGuid == facilityGuid
						 select items).OrderBy(click => click.ClickGuid);
				}
			}
			catch (System.Data.SqlClient.SqlException ex)
			{
				throw this.HandleSqlException(ex);
			}
		}

		public IOrderedQueryable<Click> GetForListingTypeByListingTypeGuid(Guid listingTypeGuid)
		{
			try
			{
				using (PSS2012DataContext context = DataContext)
				{
					// Get the entity to delete.
					return
						(from items in context.Clicks
						 where items.ListingTypeGuid == listingTypeGuid
						 select items).OrderBy(click => click.ClickGuid);
				}
			}
			catch (System.Data.SqlClient.SqlException ex)
			{
				throw this.HandleSqlException(ex);
			}
		}
		#endregion


		#region Private Handle Exception Methods
		private Exception HandleSqlException(System.Data.SqlClient.SqlException ex)
		{
			if (ex.Message.Contains("An error has occurred while establishing a connection to the server.  When connecting to SQL Server 2005, this failure may be caused by the fact that under the default settings SQL Server does not allow remote connections."))
			{
				return new Exception("Could not establish a connection to SQL Server. See inner exception for more details. Perhaps Windows Firewall has blocked the connection to SQL Server. Check online resources to find out how to allow port exceptions.", ex);
			}
			else if (ex.Message.Contains("Invalid object name 'dbo.Click'"))
			{
				return new TableDoesNotExistException("PSS2012", "Click", ex);
			}
			else if (ex.Message.Contains("Invalid column name"))
			{
				return new ColumnDoesNotExistException("PSS2012", "Click", ex);
			}
			else if (ex.Message.Contains("String or binary data would be truncated"))
			{
				return new DataAccessException("String or number is too long or too big for the database.", ex);
			}
			else if (ex.Message.Contains("The DELETE statement conflicted with the REFERENCE constraint"))
			{
				Regex errorRx = new Regex("The DELETE statement conflicted with the REFERENCE constraint \"(?<constraint>[\\x20-\\x7E-[\"]]{1,})\". The conflict occurred in database \"(?<db>[\\x20-\\x7E-[\"]]{1,50})\", table \"dbo.(?<jtn>[\\x20-\\x7E-[\"]]{1,50})\"");
				Match errorMatch = errorRx.Match(ex.Message);
				if (errorMatch.Success)
				{
					return new RowReferencedElsewhereException(errorMatch.Groups["db"].Value, "Click", errorMatch.Groups["jtn"].Value, ex);
				}
				else
				{ return ex; }
			}
			else
			{
				return ex;
			}
		}
		#endregion
	}
}